<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="10dp"
    android:background="@drawable/bg_grey_corner_radius"
    android:padding="10dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/clHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tvConnectedTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginEnd="10dp"
            android:fontFamily="@font/poppins_medium"
            android:text="Business Checking"
            android:textColor="@color/heading_color"
            android:textSize="16sp"
            app:layout_constraintEnd_toStartOf="@+id/tvConnectedDate"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/tvConnectedDate"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentEnd="true"
            android:fontFamily="@font/poppins_regular"
            android:text="Updated 5 hours ago"
            android:textColor="@color/grey_text"
            android:textSize="10sp"
            app:layout_constraintBaseline_toBaselineOf="@+id/tvConnectedTitle"
            app:layout_constraintRight_toRightOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <TextView
        android:id="@+id/tvConnectedCreditCard"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/clHeader"
        android:layout_marginEnd="10dp"
        android:layout_toStartOf="@+id/tvConnectedAmount"
        android:fontFamily="@font/poppins_regular"
        android:text="************560"
        android:textColor="@color/heading_color"
        android:textSize="16sp" />

    <TextView
        android:id="@+id/tvConnectedAmount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/clHeader"
        android:layout_alignParentEnd="true"
        android:fontFamily="@font/poppins_regular"
        android:text="$16304.56"
        android:textColor="@color/heading_color"
        android:textSize="16sp" />
</RelativeLayout>